# CRM для управления многомиллионными базами номеров

## Описание проекта

Enterprise CRM-система для управления крупномасштабными базами телефонных номеров (миллионы записей) с распределением по проектам, поставщикам и гео, поддержкой конверсионной аналитики и интеграций (SFTP, SoftPhone).

## Архитектура

- **Backend**: Python + Django REST Framework + PostgreSQL + Celery/Redis
- **Frontend**: React + TypeScript + Material-UI
- **Деплой**: Docker Compose (локальный старт), с возможностью переноса в облако

## Ключевой функционал

1. **Управление проектами**: создание, редактирование, удаление (мягкое) проектов с привязкой к поставщикам, гео и расписанию.
2. **Выбор поставщика/гео/дней недели**: гибкая настройка параметров проекта.
3. **Конверсия проектов**: агрегированные метрики по обработке номеров и конверсии.
4. **Удалённые проекты**: просмотр, восстановление и окончательное удаление.
5. **Выгрузка и импорт номеров**: массовые операции с CSV/XLS, интеграция SFTP.
6. **Интеграция SoftPhone**: webhook для событий звонков, привязка к номерам.
7. **Роли и права**: Администратор (полный доступ), Менеджер (ограниченный).
8. **Аудит-лог**: запись всех действий пользователей и системных задач.

## Структура документации

### Основные файлы

- **[architecture_overview.md](./architecture_overview.md)** — общая архитектура системы, сущности, роли, интеграции, выбор стека.
- **[backend_plan_dev1.md](./backend_plan_dev1.md)** — технический план для backend-разработчика (схема БД, модули, API, спринты).
- **[frontend_plan_dev2.md](./frontend_plan_dev2.md)** — технический план для frontend-разработчика (компоненты, экраны, интеграция с API, спринты).
- **[api_contracts.md](./api_contracts.md)** — описание REST API эндпоинтов, форматов запросов/ответов, кодов ошибок.

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose (для локального запуска)
- Git
- Node.js 20+ (для frontend разработки)
- Python 3.11+ (для backend разработки)

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/fake-iac/Test.git
cd Test
```

2. Запустите инфраструктуру через Docker Compose:
```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL (порт 5432)
- Redis (порт 6379)
- Backend API (порт 8000)
- Frontend (порт 3000)

3. Примените миграции БД:
```bash
docker-compose exec backend python manage.py migrate
```

4. Создайте суперпользователя:
```bash
docker-compose exec backend python manage.py createsuperuser
```

5. Откройте приложение:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000/api
- Admin панель: http://localhost:8000/admin

### Разработка

#### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python manage.py runserver
```

#### Frontend

```bash
cd frontend
npm install
npm run dev
```

## Роли и доступ

### Администратор

- Полный CRUD на все сущности
- Доступ к настройкам, интеграциям, аудит-логу
- Управление пользователями и ролями

### Менеджер

- Просмотр и изменение проектов и номеров в рамках разрешений
- Создание выгрузок
- Нет доступа к системным настройкам и аудиту

## Технические особенности

### Работа с большими объемами данных

- **Индексы**: по всем ключевым полям (phone_number, project_id, status, created_at)
- **Серверная пагинация**: по 50-100 записей на страницу
- **Batched операции**: bulk_create, bulk_update в Django ORM
- **Фоновые задачи**: Celery для импорта/экспорта и расчета метрик
- **Connection pooling**: pgbouncer для PostgreSQL

### Интеграции

#### SFTP

- Настраиваемые SFTP-хосты (адрес, порт, аутентификация по ключу/паролю)
- Планировщик задач (cron-like) или ручной запуск
- Логирование успешных/неудачных операций

#### SoftPhone

- Webhook endpoint для приема событий звонков
- Автоматическая привязка событий к номерам по phone_number
- Расширяемость для pop-up уведомлений и записи результатов

### Аудит-логирование

- Автоматическая запись всех CRUD операций
- Хранение изменений в JSON (old_value, new_value)
- Привязка к пользователю, IP-адресу и user-agent
- Фильтрация и экспорт для аналитики

## Использование ИИ-помощников

Проект разработан с учетом использования ИИ-помощников для:

- Генерации типового кода (serializers, ViewSets, компоненты React)
- Создания миграций БД и SQL-запросов
- Написания unit и integration тестов
- Обновления документации при изменении API

**Важно**: Все решения и код, предложенные ИИ, проходят ручное ревью и тестирование перед включением в основной код.

## Этапы разработки

### Фаза 1: MVP (6-8 недель)

- ✅ Базовая инфраструктура и аутентификация
- ✅ Управление проектами, поставщиками, гео
- ✅ Управление номерами с фильтрацией
- ✅ Конверсия проектов (базовая)
- ✅ Удалённые проекты
- ✅ Импорт/экспорт CSV
- ✅ Роли и права (Администратор, Менеджер)

### Фаза 2: Интеграции (3-4 недели)

- 🔲 SFTP интеграция (импорт/экспорт файлов)
- 🔲 SoftPhone webhook и события звонков
- 🔲 Аудит-логирование
- 🔲 Расширенная аналитика конверсии

### Фаза 3: Масштабирование (2-3 недели)

- 🔲 Оптимизация для >10 млн номеров
- 🔲 Партиционирование таблицы phone_numbers
- 🔲 Настройка production окружения
- 🔲 Мониторинг и логирование (Prometheus, Grafana)

## Тестирование

### Backend

```bash
cd backend
pytest tests/ --cov=. --cov-report=html
```

### Frontend

```bash
cd frontend
npm run test
npm run test:coverage
```

## Деплой в production

### Вариант 1: VPS/Dedicated Server

1. Настройте сервер с Docker
2. Клонируйте репозиторий
3. Настройте `.env` файлы с production параметрами
4. Запустите `docker-compose -f docker-compose.prod.yml up -d`
5. Настройте Nginx для reverse proxy и SSL (Let's Encrypt)

### Вариант 2: Cloud (AWS/GCP/Azure)

1. Разверните PostgreSQL как managed service (RDS, Cloud SQL)
2. Разверните Redis как managed service (ElastiCache, Memorystore)
3. Разверните backend на ECS/Cloud Run/App Service
4. Разверните frontend на S3 + CloudFront / Cloud Storage + CDN
5. Настройте CI/CD pipeline (GitHub Actions, GitLab CI)

## Поддержка и контакты

Для вопросов по архитектуре и разработке:
- Создайте Issue в репозитории
- Обратитесь к технической документации в директории `/docs`

## Лицензия

Проект разработан для внутреннего использования. Все права защищены.

---

**Версия**: 1.0.0  
**Дата создания**: 25 февраля 2026  
**Статус**: В разработке (MVP)
